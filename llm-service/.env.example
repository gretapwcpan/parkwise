# IMPORTANT: API keys are now managed in backend/.env
# Please set OPENAI_API_KEY or ANTHROPIC_API_KEY in backend/.env instead
# This service will inherit those environment variables from docker-compose.yml

# Service Configuration
PORT=8001
HOST=0.0.0.0
DEBUG=true

# CORS Settings
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Logging
LOG_LEVEL=INFO

# Note: The following environment variables are now set in backend/.env:
# - OPENAI_API_KEY
# - ANTHROPIC_API_KEY
# - LLM_MODEL (defaults to gpt-4 if not set)
