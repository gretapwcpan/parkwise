# This is a template file. DO NOT commit actual secrets to git.
# Create actual secrets using kubectl or your cloud provider's secret management.

apiVersion: v1
kind: Secret
metadata:
  name: parkwise-secrets
  namespace: parkwise
  labels:
    app: parkwise
type: Opaque
data:
  # Base64 encoded values (use: echo -n "value" | base64)
  
  # API Keys (replace with actual base64 encoded values)
  OPENAI_API_KEY: ""  # echo -n "your-openai-key" | base64
  ANTHROPIC_API_KEY: ""  # echo -n "your-anthropic-key" | base64
  LLM_API_KEY: ""  # echo -n "your-llm-api-key" | base64
  
  # Database credentials
  # Add your database credentials here if needed
  
  # Firebase configuration
  FIREBASE_PROJECT_ID: ""  # echo -n "your-firebase-project-id" | base64
  FIREBASE_PRIVATE_KEY: ""  # echo -n "your-firebase-private-key" | base64
  FIREBASE_CLIENT_EMAIL: ""  # echo -n "your-firebase-client-email" | base64

---
# Example of how to create secrets via kubectl:
# kubectl create secret generic parkwise-secrets \
#   --from-literal=OPENAI_API_KEY="your-openai-key" \
#   --from-literal=ANTHROPIC_API_KEY="your-anthropic-key" \
#   --from-literal=FIREBASE_PROJECT_ID="your-firebase-project" \
#   -n parkwise