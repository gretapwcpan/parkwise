# Core dependencies (required for all modes)
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-dotenv==1.0.0
pydantic==2.5.3
requests==2.32.3

# OpenAI-compatible API mode (includes Ollama support)
openai>=1.0.0

# vLLM mode (GPU)
vllm==0.5.0
torch>=2.0.0
transformers>=4.40.0
accelerate>=0.27.0

# Optional but recommended
numpy>=1.24.0
